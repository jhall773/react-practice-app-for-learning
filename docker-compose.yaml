version: '3.9'

services:
  simple-api:
    build: ./simple_api
    container_name: simple-api
    ports:
      - "3001:3001"

  react-app:
    build: ./react_app
    container_name: react-app
    ports:
      - "3000:80"
    depends_on:
      - simple-api